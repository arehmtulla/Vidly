name: test-github-actions
on: [push]
jobs:
  ssh-into-ami:
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: 54.184.44.174
        username: Administrator
        password: YNUdP4AMCYNzG4ETBB62XWCBruzkcV
        script: |
          mkdir vidlytest
      